import React from 'react';

export function SeoHead({ title, description, canonicalUrl, ogImage }) {
  return null; // For now we'll handle SEO dynamically or render react-helmet/meta attributes.
}

export default SeoHead;
